<p id="notice"><%= notice %></p>

<p>
  <%= link_to 'Add Notion Calendar', new_sync_notion_sync_source_path(sync_id: @sync.id) %>
</p>

<%= link_to 'Edit', edit_sync_path(@sync) %> |
<%= link_to 'Back', syncs_path %>
